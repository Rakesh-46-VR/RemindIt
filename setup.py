from setuptools import setup, find_packages

setup(
    name="auth",   # The name of your module
    version="0.1",                 # Module version
    packages=find_packages(),      # Automatically find all packages in the project
)